

const path = (root, sublink) => {
  return `${root}${sublink}`;
};

export const PATH_PAGE = {
    
    aboutMe:'/',
    skills:'/skills',
    projects:'/projects',
    services:'/services',
    contact:'/contact',

}